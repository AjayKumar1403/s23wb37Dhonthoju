extends layout

block content
  h1= title
  -let results =["img1.jpg", "img2.jpg", "img3.jpg", "img4.jpg", "img5.jpg"]
  -for(var i=0;i<3; i++) {
    -let randonnum = Math.floor(Math.random()*5)
    img(src=`images/${results[randonnum]}`,height="300px",width="300px")
  -}